import React from 'react';

const ProductUser = ({product}) => {
    return (
        <div>
            {
                product.name
            }
        </div>
    );
};

export default ProductUser;